// Install json-server from npm by running the command: npm install -g json-server
// Create a new file named "albums.json" and copy the provided sample data into it
// Run the command: json-server --watch albums.json to start the json-server and use the "albums.json" file as the data source.